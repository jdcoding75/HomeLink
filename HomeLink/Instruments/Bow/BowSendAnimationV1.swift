// BowSendAnimationV1.swift
// Pointward › Instruments › Bow
//
// ⛔️ RETIRED (2026-06-11). The bow was rebuilt to the approved visual bible and
// V2 (BowSendAnimationV2) is now the LIVE send — promoted in CompassView's send
// dispatch. The old V1 path (SenderAnimationView.bowArrowSend) is no longer the
// active bow send. This marker is kept (not deleted) per the framework's
// "V1 always kept" rule; its body is commented out as it is now unreferenced.

import SwiftUI

// enum BowSendAnimationV1 {
//     static let duration: Double = InstrumentBoundaries.Send.bow
//     static let isActive = false   // RETIRED — V2 is live
// }
